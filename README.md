# 🤖 AI Agent - Терминальный/Telegram ассистент

Локальный AI-агент, работающий через OpenAI API, с множеством встроенных инструментов для выполнения различных задач.

## 🚀 Быстрый старт

### 1. Создание виртуального окружения
**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 2. Установка зависимостей
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 3. Настройка конфигурации
Скопируйте файл `env-example` в `.env`:

**Windows:**
```bash
copy env-example .env
```

**Linux/macOS:**
```bash
cp env-example .env
```

Отредактируйте файл `.env` и вставьте ваш API ключ:
```bash
# OpenAI API Configuration
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1

# Model configuration
OPENAI_MODEL=gpt-5-mini
```

**Примечание:** Проект настроен для использования proxy API (proxyapi.ru). Вы можете использовать официальный OpenAI API, удалив строку `OPENAI_BASE_URL`.

### 4. Запуск агента
```bash
python run.py
```

## 📋 Возможности

### 🌤️ Погода
- "Какая погода в Москве?"
- "Weather in New York"

### 💰 Криптовалюты
- "Цена биткоина"
- "Сколько стоит Ethereum в рублях?"

### 💱 Курсы валют
- "Курс доллара к рублю"
- "Сконвертируй 100 EUR в USD"
- "Курс евро"

### 🔍 Поиск в интернете
- "Найди информацию о Python"
- "Что такое машинное обучение?"

### 📁 Работа с файлами
- "Покажи содержимое файла README.md"
- "Создай файл notes.txt с текстом 'Мои заметки'"
- "Покажи файлы в директории temp"

### 🌐 HTTP запросы
- "Сделай GET запрос на https://api.github.com/user"

### 💻 Терминальные команды
- "Выполни команду ls -la"
- "Показать текущую директорию"

### 📱 QR-коды
- "Создай QR-код с текстом 'Hello World'"
- "Сгенерируй QR-код для ссылки https://google.com"

### 📝 Напоминания
- "Напомни купить молоко"
- "Добавь напоминание позвонить маме на 2024-12-25 18:00"
- "Покажи мои напоминания"
- "Удали напоминание #1"

### 🧮 Калькулятор
- "Посчитай 2^10 + sqrt(144)"
- "Вычисли sin(pi/2)"
- "Чему равно (15 * 8) / 4"

### 📱 Telegram интерфейс
- Удобное общение через мессенджер
- История разговора для каждого пользователя
- Автоматическое разбиение длинных сообщений
- Поддержка команд /start, /help, /clear, /history

## 🛠️ Структура проекта

```
agent/
├── agent.py          # Логика агента и LangChain интеграция
├── tools.py          # Все доступные инструменты
├── run.py            # CLI интерфейс запуска
├── bot.py            # Telegram бот интерфейс
├── memory.json       # История разговора
├── temp/             # Папка для сохранения файлов
├── .env              # API ключи (создается из env-example)
├── env-example       # Шаблон конфигурации
├── .gitignore        # Игнорируемые файлы
├── requirements.txt  # Зависимости Python
└── README.md         # Эта документация
```

## 🎯 Использование

**⚠️ Важно:** Перед запуском активируйте виртуальное окружение!

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/macOS:**
```bash
source venv/bin/activate
```

### 🖥️ Терминальный режим
```bash
python run.py
```

### 📱 Telegram бот
```bash
python bot.py
```

**Настройка Telegram бота:**
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Скопируйте токен бота
3. Добавьте токен в `.env`:
   ```bash
   TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
   ```
4. Запустите бота: `python bot.py`
5. Найдите вашего бота в Telegram и начните общение!

### Одиночный запрос
```bash
python run.py --query "Какая погода в Москве?"
```

### Упрощенная версия (без LangChain)
```bash
python run.py --simple
```

### Выбор модели
```bash
python run.py --model gpt-3.5-turbo
```

## 📚 Команды

### Терминальный режим
- `help` - показать справку
- `clear` - очистить память разговора
- `history` - показать историю разговора
- `exit` или `quit` - выйти

### Telegram бот
- `/start` - начать работу с ботом
- `/help` - показать справку
- `/clear` - очистить историю разговора
- `/history` - показать последние 10 сообщений

## 🔧 Конфигурация

### Переменные окружения (.env)
```bash
# Обязательные параметры
OPENAI_API_KEY=ваш_api_ключ
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1

# Опциональные параметры
OPENAI_MODEL=gpt-5-mini
OPENAI_TEMPERATURE=0.7
```

**Параметры:**
- `OPENAI_API_KEY` - ваш API ключ
- `OPENAI_BASE_URL` - URL proxy API (опционально, для официального OpenAI API удалите эту строку)
- `OPENAI_MODEL` - модель для использования (по умолчанию gpt-5-mini)
- `OPENAI_TEMPERATURE` - температура генерации (0.0-1.0, по умолчанию 0.7)

### Безопасность терминальных команд
Агент выполняет только безопасные команды из белого списка:
- `ls`, `dir`, `pwd`, `echo`, `cat`
- `python`, `pip`, `git`, `docker`
- И другие безопасные команды

## 🛡️ Безопасность

- **API ключи**: хранятся локально в `.env` файле
- **Терминальные команды**: ограничены белым списком безопасных команд
- **Файловый доступ**: чтение/запись только в разрешенные директории
- **HTTP запросы**: с таймаутами и обработкой ошибок

## 🆕 Детальное описание новых инструментов

### 💱 Курсы валют (`get_exchange_rate`)

**API:** 
- ЦБ РФ API для RUB (бесплатный, без ключа)
- Frankfurter API для остальных валют (бесплатный, без ключа)

**Поддерживаемые валюты:**
- USD, EUR, GBP, JPY, CHF
- RUB, UAH, BYN, KZT
- CNY, INR, BRL и другие

**Примеры:**
```
"Курс доллара к рублю"
"Сконвертируй 100 EUR в USD"
"Сколько стоит 50 USD в RUB?"
```

**Результат:**
```
💱 Курс обмена (ЦБ РФ):
1 USD = 92.5432 RUB

📊 Конвертация:
50 USD = 4627.16 RUB

📅 Дата: 2026-01-19
```

### 📱 QR-коды (`generate_qr_code`)

**Библиотека:** `qrcode[pil]`

**Возможности:**
- Генерация QR-кодов из любого текста
- Автоматическое сохранение в `temp/`
- Поддержка URL, текста, контактов

**Примеры:**
```
"Создай QR-код с текстом Hello World"
"Сгенерируй QR для ссылки https://google.com"
```

**Результат:** PNG файл в `temp/qr_code.png`

### 📝 Напоминания (3 инструмента)

**Функции:**
- `add_reminder(text, date_time)` - добавить напоминание
- `list_reminders(show_completed)` - показать список
- `delete_reminder(reminder_id)` - удалить по ID

**Хранение:** `temp/reminders.json`

**Примеры:**
```
"Напомни купить молоко"
"Добавь напоминание позвонить маме на 2024-12-25 18:00"
"Покажи мои напоминания"
"Удали напоминание #1"
```

**Результат:**
```
✅ Напоминание #1 добавлено на 2024-12-25 18:00:
Позвонить маме
```

### 🧮 Калькулятор (`calculate`)

**Библиотека:** `sympy` (безопасные математические вычисления)

**Поддержка:**
- **Операции:** `+`, `-`, `*`, `/`, `**` (степень)
- **Функции:** `sqrt()`, `sin()`, `cos()`, `tan()`, `log()`, `exp()`
- **Константы:** `pi`, `e`, `oo` (бесконечность)

**Примеры и результаты:**
```
"Посчитай 2^10 + sqrt(144)"  
→ Результат: 2**10 + sqrt(144) = 1036

"Вычисли sin(pi/2)"
→ Результат: sin(pi/2) = 1

"Чему равно (15 * 8) / 4"
→ Результат: (15*8)/4 = 30
```

## 🔍 Примеры запросов

### Погода
```
Какая погода в Санкт-Петербурге?
Weather in Tokyo
```

### Криптовалюты
```
Цена биткоина
Сколько стоит Ethereum в евро?
get_crypto_price cardano usd
```

### Поиск информации
```
Найди последние новости о AI
Что такое нейронные сети?
web_search python async programming
```

### Работа с файлами
```
Покажи содержимое файла config.json
Создай файл todo.txt с текстом 'Купить продукты'
read_file requirements.txt
write_file notes.md '# Мои заметки'
list_directory /home/user/Documents
```

### Терминальные команды
```
Выполни команду pwd
run_terminal_command ls -la
Показать текущую директорию
```

## 🐛 Устранение неполадок

### Ошибка "OPENAI_API_KEY не найден"
1. Проверьте файл `.env`
2. Убедитесь, что ключ правильный
3. Перезапустите терминал

### Ошибка подключения к интернету
- Проверьте интернет-соединение
- Для инструментов погоды/крипты/поиска нужен интернет

### Ошибка выполнения команд
- Некоторые команды могут быть заблокированы для безопасности
- Используйте только разрешенные команды

## 📈 Технологии

Проект использует:
- **OpenAI API (или ProxyAPI)** с моделью gpt-5-mini для понимания естественного языка
- **LangChain** для оркестрации инструментов
- **DuckDuckGo** для поиска в интернете
- **Open-Meteo API** для погоды (бесплатный, без API ключа)
- **CoinGecko API** для криптовалют (бесплатный, без API ключа)
- **ЦБ РФ API** для курсов валют с рублем (бесплатный, без API ключа)
- **Frankfurter API** для остальных курсов валют (бесплатный, без API ключа)
- **qrcode** для генерации QR-кодов
- **sympy** для математических вычислений

## 📄 Лицензия

Этот проект создан для образовательных целей. Используйте ответственно и в соответствии с правилами использования API сервисов.

---

**Приятного использования! 🤖**
